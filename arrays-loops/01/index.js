const numeros = [2, 3, 4, 6];
let soma = 0;

for (let i = 0; i < numeros.length; i++) {
    soma += numeros[i];
}

console.log(soma)

//Correção do professor

//for (let item of numeros) {
    //somaToal = somaTotal + item
//}